package au.com.addstar.serversigns.taskmanager.tasks;

public enum PlayerActionTaskType {
    CHAT, MESSAGE, COMMAND, OP_COMMAND, BLANK_MESSAGE, SERVER_COMMAND, ADD_GROUP, DEL_GROUP, CANCEL_TASKS;

    PlayerActionTaskType() {
    }
}